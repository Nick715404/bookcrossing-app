import { api } from "../../axios/axiosInstance";
import { createEffect } from "effector";

export const PushBookToShelf = createEffect(async (bookId: string) => {
  try {
    
  }
  catch (error) {
    throw error;
  }
});